const name ="prajjwal" // not changeable
let age =12 // if any variable need to change use let
var ss = 1 // 1.prevent use of var  as it has function scope instead of block scope 
          //  2.vairbale with the same name can be redeclared that will lead to overwrite of the values.
